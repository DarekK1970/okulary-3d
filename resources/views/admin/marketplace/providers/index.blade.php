@extends('admin.layout')
@section('title', __('marketplace.admin.providers.title'))
@section('content')
<section class="market-admin"><header class="market-head"><div><span class="admin-eyebrow">MARKETPLACE</span><h1>{{ __('marketplace.admin.providers.title') }}</h1><p>{{ __('marketplace.admin.providers.description') }}</p></div><a class="market-button" href="{{ route('admin.marketplace.providers.create') }}">+ {{ __('marketplace.admin.providers.new') }}</a></header>
<div class="market-grid">@forelse($providers as $provider)<article class="market-card"><h2>{{ $provider->name }}</h2><p>{{ __('marketplace.admin.common.'.($provider->is_active ? 'active' : 'inactive')) }}</p><ul>@foreach($provider->rates as $rate)<li>{{ $rate->country_code ?: __('marketplace.admin.providers.other_countries') }} · {{ $rate->print_size ?: __('marketplace.admin.providers.all_formats') }}: <strong>{{ $rate->token_cost }} TL</strong></li>@endforeach</ul><div class="market-actions"><a class="market-button is-light" href="{{ route('admin.marketplace.providers.edit', $provider) }}">{{ __('marketplace.admin.common.edit') }}</a><form method="post" action="{{ route('admin.marketplace.providers.destroy', $provider) }}" onsubmit="return confirm('{{ __('marketplace.admin.common.delete_confirm') }}')">@csrf @method('DELETE')<button class="market-button is-danger">{{ __('marketplace.admin.common.delete') }}</button></form></div></article>@empty<article class="market-card">{{ __('marketplace.admin.providers.empty') }}</article>@endforelse</div></section>
@endsection
